<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="channel">
	<resultMap type="Channel" id="ChannelMap">
		<result property="tblId" column="tbl_id"/>
		<result property="channelName" column="channel_name"/>
		<result property="status" column="status"/>
		<result property="createdDate" column="created_date"/>
		<result property="createdUser" column="created_user"/>
		<result property="updatedDate" column="updated_date"/>
		<result property="updatedUser" column="updated_user"/>
		<result property="domainId" column="domain_id"/>
	</resultMap>
	<!-- 获取所有有效的域名 -->
	<select id="getAllAvailableChannelByDomain" resultMap="ChannelMap" parameterType="string">
		select 
			tbl_id,
			channel_name,
			domain_id,
			status
		from tbl_dw_dim_game_channel
		where status = '0'
			and domain_id = #{domainId}
        order by created_date
	</select>
</mapper>