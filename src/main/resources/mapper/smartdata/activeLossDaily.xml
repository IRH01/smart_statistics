<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="activeLossDaily">
    <resultMap type="ActiveLossDaily" id="ActiveLossDailyMap" extends="lossDaily.LossDailyMap">
        <result property="activeCount" column="active_count"/>
    </resultMap>

    <select id="find" resultMap="ActiveLossDailyMap" parameterType="map">
        select
        stat_date,
        platform_id,
        active_count,
        seven,
        fourteen,
        thirty,
        create_time,
        update_time
        from gp_tbl_active_loss_daily
        where 1=1
        <if test="platformId != null">and platform_id = #{platformId}</if>
        <if test="startDate != null and endDate != null">and date_format(stat_date,'%Y-%m-%d') between #{startDate} and
            #{endDate}
        </if>
        order by stat_date
    </select>
</mapper>